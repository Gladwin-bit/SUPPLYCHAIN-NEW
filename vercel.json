{
  "framework": "create-react-app",
  "installCommand": "cd frontend && npm install --legacy-peer-deps",
  "buildCommand": "cd frontend && GENERATE_SOURCEMAP=false CI=false npm run build && cd .. && mv frontend/build build"
}
